<H1>Portfolio Items <%= "(#{@subtitle})" if @subtitle.present? %></H1>

<%= link_to "Create new portfolio", new_portfolio_path if logged_in?(:site_admin) %>

<%= render partial: 'portfolio_item', collection: @portfolio_items, spacer_template: 'portfolio_item_spacer' %>